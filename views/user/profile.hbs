<div class="container">
    <h1><strong>Perfil de {{user.name}}</strong> </h1>

    <hr>
    <img src="{{user.cover}}" class="img-fluid rounded-start" alt="...">
    <p style="font-size: x-large;">UserName: {{user.username}}</p>
    <p style="font-size: x-large;">Fecha de Nacimiento: {{user.age}}</p>
    <p style="font-size: x-large;">Email: {{user.email}}</p>
    <a href="/perfil/editar" class="btn btn-dark"> Editar </a>

    <hr>

    <a class="btn btn-dark" href="/">volver al inicio</a>

</div>